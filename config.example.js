// Chat View Configuration
// Copy this file to config.js and fill in your actual values.
// config.js is gitignored and should never be committed.
window.CHAT_VIEW_CONFIG = {
  // Your Supabase project subdomain (e.g. 'abcdefghij' from abcdefghij.supabase.co)
  projectId: 'your-project-id',
  // Your Supabase project's anon/public key
  anonKey: 'your-anon-key',
  // Restrict sign-in to specific email domains. Leave empty to allow all Google accounts.
  // Example: ['yourcompany.com', 'partner.com']
  allowedDomains: [],
};
